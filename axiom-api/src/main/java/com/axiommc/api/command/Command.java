package com.axiommc.api.command;

/**
 * Base interface for commands.
 *
 * <p>All command implementations must implement this interface. Commands are
 * registered with the {@link CommandRegistry} and executed by the command system.
 */
public interface Command {
}
